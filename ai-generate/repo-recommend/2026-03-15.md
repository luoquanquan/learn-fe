# 2026-03-15 GitHub 项目推荐

> 每日精选 5 个值得关注的开源项目

---

## 1. [ TanStack Query](https://github.com/TanStack/query)

**类型**: 数据获取库 | **语言**: TypeScript | **⭐**: 42.1k

### 简介

强大的异步状态管理库，用于 TS/JS、React、Solid、Vue 和 Svelte。

### 特点

- 🔄 自动缓存和后台刷新
- 🎯 窗口聚焦自动重新获取
- 📡 分页和无限滚动支持
- 🚀 乐观更新

### React 示例

```typescript
import { useQuery, useMutation } from '@tanstack/react-query'

// 查询数据
function useUser(userId: string) {
  return useQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId),
    staleTime: 5 * 60 * 1000, // 5分钟
  })
}

// 修改数据
const mutation = useMutation({
  mutationFn: updateUser,
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['user'] })
  },
})
```

---

## 2. [foundry-rs/foundry](https://github.com/foundry-rs/foundry)

**类型**: 智能合约开发框架 | **语言**: Rust | **⭐**: 34.2k

### 简介

极速、可移植、模块化的以太坊应用开发工具包。

### 特点

- ⚡ 极快的测试速度（Rust 编写）
- 📝 Solidity 原生测试
- 🔧 强大的作弊码（Cheatcodes）
- 📊 Gas 快照报告

### 快速开始

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
forge init hello_foundry
```

```solidity
// 测试示例
contract ContractTest is Test {
  function setUp() public {}
  
  function test_Increment() public {
    counter.increment();
    assertEq(counter.number(), 1);
  }
  
  function testFuzz_SetNumber(uint256 x) public {
    counter.setNumber(x);
    assertEq(counter.number(), x);
  }
}
```

---

## 3. [pinecone-io/pinecone-ts-client](https://github.com/pinecone-io/pinecone-ts-client)

**类型**: 向量数据库客户端 | **语言**: TypeScript | **⭐**: 1.2k

### 简介

Pinecone 官方 TypeScript 客户端，用于构建向量搜索应用。

### 特点

- 🔍 高性能向量搜索
- 🎯 元数据过滤
- 🚀 实时更新
- 📦 托管服务，无需运维

### 示例

```typescript
import { Pinecone } from '@pinecone-database/pinecone'

const pc = new Pinecone({ apiKey: '...' })
const index = pc.index('my-index')

// 插入向量
await index.upsert([{
  id: '1',
  values: [0.1, 0.2, 0.3, ...], // 1536维
  metadata: { genre: 'action' }
}])

// 查询相似向量
const results = await index.query({
  vector: [0.1, 0.2, 0.3, ...],
  topK: 5,
  filter: { genre: 'action' }
})
```

---

## 4. [unjs/nitro](https://github.com/unjs/nitro)

**类型**: 服务端引擎 | **语言**: TypeScript | **⭐**: 6.8k

### 简介

为现代 Web 应用打造的通用服务端引擎，Nuxt 3 的底层引擎。

### 特点

- 🌐 多平台部署（Node、Deno、Cloudflare、Vercel 等）
- 📦 自动代码分割
- 🔌 插件系统
- 🎯 类型安全 API 路由

### 示例

```typescript
// 定义 API 路由
export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  
  return {
    message: `Hello ${body.name}!`,
    timestamp: Date.now()
  }
})
```

---

## 5. [reflex-dev/reflex](https://github.com/reflex-dev/reflex)

**类型**: Python Web 框架 | **语言**: Python | **⭐**: 19.5k

### 简介

纯 Python 构建响应式 Web 应用，无需 JavaScript。

### 特点

- 🐍 纯 Python，无需 JS
- 🎯 声明式 UI
- 🚀 自动状态管理
- 📦 一键部署

### 示例

```python
import reflex as rx

class State(rx.State):
    count: int = 0
    
    def increment(self):
        self.count += 1

def index():
    return rx.hstack(
        rx.button(
            "Decrement",
            on_click=State.decrement,
        ),
        rx.heading(State.count, font_size="2em"),
        rx.button(
            "Increment", 
            on_click=State.increment,
        ),
    )

app = rx.App()
app.add_page(index)
```

---

## 今日主题

**数据管理 + Web3 工具 + AI 基础设施**

| 项目 | 方向 | 亮点 |
|------|------|------|
| TanStack Query | 数据 | 异步状态管理的事实标准 |
| Foundry | Web3 | 最快智能合约测试框架 |
| Pinecone | AI | 领先的向量数据库 |
| Nitro | 后端 | 通用服务端引擎 |
| Reflex | Python | 纯 Python 构建 Web 应用 |

---

*推荐时间: 2026-03-15 20:00*
*来源: GitHub Trending & 社区精选*
