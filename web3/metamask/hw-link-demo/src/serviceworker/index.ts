/**
 * your code here
 */

export {}; // stops ts error that the file isn't a module

console.log(`Current timestamp ${Date.now()} chrome.hid: `, chrome.hid)

console.log(`Current timestamp ${Date.now()}: service worker ~`);
